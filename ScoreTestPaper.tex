%% Paper implementing score test framework for SCR :)

% %% Uncomment to knit for Ben and Rachel:
% \documentclass[14pt]{extarticle}
% \usepackage{geometry}
% \geometry{left=20mm, right=20mm, top=5mm, bottom=5mm,
%  headheight=5mm, marginpar=5mm}

%% Uncomment to knit for writing: 
\documentclass{article}
\usepackage{geometry}
\geometry{left=15mm, right=15mm, top=5mm, bottom=5mm,
 headheight=5mm, marginpar=5mm}

\usepackage{layout}
\usepackage{blindtext}
\usepackage{amsmath}
\usepackage{xcolor}
\usepackage{multicol}
\usepackage{multirow}
\usepackage{tabularx}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{graphicx}
\usepackage{amsfonts}
\usepackage[parfill]{parskip} % Want line break between paragraphs instead of indentation
\usepackage{setspace} % For line spacing
\usepackage{lineno,xcolor} % For numbering lines
\usepackage{arydshln}

 % For R code, for now :)
\usepackage{listings}
\lstdefinelanguage{R}{
  keywords={if, else, for, while, function, return},
  otherkeywords={TRUE, FALSE, NULL, NA},
  sensitive=true,
  morecomment=[l]{\#},
  morestring=[b]"
}

\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\lstset{
  backgroundcolor=\color{backcolour},
  language=R,
  basicstyle=\ttfamily,
  keywordstyle=\bfseries,
  commentstyle=\itshape,
  stringstyle=\ttfamily
}
% \definecolor{codegreen}{rgb}{0,0.6,0}
% \definecolor{codegray}{rgb}{0.5,0.5,0.5}
% \definecolor{codepurple}{rgb}{0.58,0,0.82}
% \definecolor{backcolour}{rgb}{0.95,0.95,0.92}
% \lstdefinestyle{mystyle}{
%   backgroundcolor=\color{backcolour},   
%   commentstyle=\color{codegreen},
%   basicstyle=\ttfamily\footnotesize,
%   breakatwhitespace=false,         
%   breaklines=true,                 
%   captionpos=b,                    
%   keepspaces=true,                 
%   %numbers=left,                    
%   numbersep=5pt,                  
%   showspaces=false,                
%   showstringspaces=false,
%   showtabs=false,                  
%   tabsize=2
% }
% \lstset{style=mystyle}


%% Bibliography, referencing 
\usepackage[backend=biber,citestyle=authoryear,uniquename=false,maxcitenames=2,
mincitenames=1,uniquelist=false, style=apa,giveninits=true]{biblatex}
\addbibresource{../../../../../PhDReferences_Zotero.bib}
\makeatletter
\newcommand{\apamaxcitenames}{2}
\DeclareNameFormat{labelname}{%
  % First set the truncation point
  \ifthenelse{\value{uniquelist}>1}
    {\numdef\cbx@min{\value{uniquelist}}}
    {\numdef\cbx@min{\value{minnames}}}%
  % Always print the first name and the second if there are only two since
  % "et al" must always be plural
  \ifboolexpr{test {\ifnumcomp{\value{listcount}}{=}{1}}
              or test {\ifnumcomp{\value{listtotal}}{=}{2}}}
    {\usebibmacro{labelname:doname}%
      {\namepartfamily}%
      {\namepartfamilyi}%
      {\namepartgiven}%
      {\namepartgiveni}%
      {\namepartprefix}%
      {\namepartprefixi}%
      {\namepartsuffix}%
      {\namepartsuffixi}}
    % We are looking at name >=3
    % If the list is 6 or more names or we have seen citation before, potential truncation
    {\ifboolexpr{test {\ifnumcomp{\value{listtotal}}{>}{\apamaxcitenames}}
                 or test {\ifciteseen}}
     % Less than the truncation point, print normally
     {\ifnumcomp{\value{listcount}}{<}{\cbx@min + 1}
       {\usebibmacro{labelname:doname}%
         {\namepartfamily}%
         {\namepartfamilyi}%
         {\namepartgiven}%
         {\namepartgiveni}%
         {\namepartprefix}%
         {\namepartprefixi}%
         {\namepartsuffix}%
         {\namepartsuffixi}}
       {}%
      % At potential truncation point ...
      \ifnumcomp{\value{listcount}}{=}{\cbx@min + 1}
        % but enforce plurality of et al - only truncate here if there is at
        % least one more element after the current potential truncation point
        % so that "et al" covers at least two elements.
        {\ifnumcomp{\value{listcount}}{<}{\value{listtotal}}
          {\printdelim{andothersdelim}\bibstring{andothers}}
          {\usebibmacro{labelname:doname}%
            {\namepartfamily}%
            {\namepartfamilyi}%
            {\namepartgiven}%
            {\namepartgiveni}%
            {\namepartprefix}%
            {\namepartprefixi}%
            {\namepartsuffix}%
            {\namepartsuffixi}}}
        {}%
      % After truncation point, do not print name
      \ifnumcomp{\value{listcount}}{>}{\cbx@min + 1}
       {\relax}%
       {}}%
     % We are looking at name >=3
     % Name list is < 6 names or we haven't seen this citation before, print normally
     {\usebibmacro{labelname:doname}%
       {\namepartfamily}%
       {\namepartfamilyi}%
       {\namepartgiven}%
       {\namepartgiveni}%
       {\namepartprefix}%
       {\namepartprefixi}%
       {\namepartsuffix}%
       {\namepartsuffixi}}}}
\makeatother

%% Notation stuff
\newcommand{\thetaZero}{\boldsymbol{\theta}_0}
\newcommand{\thetaZeroMLE}{\hat{\boldsymbol{\theta}}_0}
\newcommand{\thetaOne}{\boldsymbol{\theta}_1}
\newcommand{\thetaOneOD}{\theta_1}
\newcommand{\thetaOneTilde}{\tilde{\boldsymbol{\theta}}_1}
\newcommand{\thetaOneTildeOD}{\tilde{\theta}_1} % OD for 'one dimension', i.e. scalar 
\newcommand{\thetaOneMLE}{\hat{\boldsymbol{\theta}}_1}
\newcommand{\thetaOneMLEOD}{\hat{\theta}_1}
\newcommand{\scoreU}{\boldsymbol{u}_1 (\thetaOneTilde)}
\newcommand{\scoreUOneDim}{u_1 (\thetaOneTildeOD)}
\newcommand{\scoreI}{\mathcal{I}_1 (\thetaOneTilde)}
\newcommand{\lr}[1]{\left( #1 \right)}
\newcommand{\lrbr}[1]{\left\{ #1 \right\}}
\newcommand{\lrsq}[1]{\left[ #1 \right]}
\newcommand{\sumN}{\sum_{i=1}^n}
\newcommand{\ellZero}{\ell_0}
\newcommand{\ellOne}{\ell_1}
\newcommand{\betaZero}{\alpha} % We replace beta0 with alpha
\newcommand{\betaZeroMLE}{\hat{\alpha}}
\newcommand{\betaOne}{\beta} % And we replace beta1 with just beta
\newcommand{\yBold}{\boldsymbol{y}}
\newcommand{\yBar}{\bar{y}}
\newcommand{\deltaBetaZero}{\frac{\delta}{\delta \alpha}}
\newcommand{\deltaBetaOne}{\frac{\delta}{\delta \beta}}
% Commands for explanations in s5.1 of Appendix: 
\newcommand{\llSimple}{\ell_1(\thetaOneOD; y)}
\newcommand{\llSimpleFirstDeriv}{\ell'_1(\thetaOneOD; y)}
\newcommand{\llSimpleSecondDeriv}{\ell''_1(\thetaOneOD; y)}
\newcommand{\pdfSimple}{f_Y (y; \thetaOneOD)}
\newcommand{\pdfSimpleTrue}{f_Y (y; \thetaOneTildeODTrue)}
\newcommand{\scoreSimpleTrue}{u_1(\thetaOneTildeODTrue)}
\newcommand{\scoreSimpleTilde}{u_1(\thetaOneTildeOD)}
\newcommand{\scoreSimpleTrueCond}{u_1(\thetaOneTildeODTrue; y)}
\newcommand{\scoreSimpleTildeCond}{u_1(\thetaOneTildeOD; y)}
\newcommand{\scoreSimple}{u_1(\thetaOneOD)}
\newcommand{\scoreSimpleCond}{u_1(\thetaOneOD; y)}
\newcommand{\deltaThetaOneOD}{\frac{\delta}{\delta \thetaOneOD}}
\newcommand{\intSimple}{\int_y}
\newcommand{\thetaOneTildeODTrue}{\tilde{\theta}_1^*}
\newcommand{\ellT}{\ell_T}
\newcommand{\thetaHatStar}{\hat{\theta}^*}
\newcommand{\ellTilde}{\ell_1(\thetaOneTildeOD)}
\newcommand{\ellTildeFirstDeriv}{\ell'_1(\thetaOneTildeOD)}
\newcommand{\ellTildeSecondDeriv}{\ell''_1(\thetaOneTildeOD)}

%% Commands for tables of results
\newcommand{\til}{$\sim$}
\newcommand{\Dconst}{D\til 1}
\newcommand{\DSession}{D\til Session}
\newcommand{\Dsession}{D\til session}
\newcommand{\gconst}{$g_0$\til 1}
\newcommand{\gSession}{$g_0$\til Session}
\newcommand{\gsession}{$g_0$\til session}
\newcommand{\gbehav}{$g_0$\til b}
\newcommand{\gsessionbehav}{$g_0$\til session + b}
\newcommand{\gmix}{$g_0$\til h2}
\newcommand{\sigmaconst}{$\sigma$\til 1}
\newcommand{\sigmaSession}{$\sigma$\til Session}
\newcommand{\sigmasession}{$\sigma$\til session}
\newcommand{\sigmamix}{$\sigma$\til h2}


\title{Harnessing score tests for model selection}

\begin{document}

\maketitle

% Setting double spacing
\doublespacing
% Adding line numbering
\linenumbers

\section{Introduction}
\label{sec:introduction}

Model selection is an important step for
statistical analysis, allowing the practitioner to confirm the
reliability of their chosen model and any resulting
conclusions. Well-known model selection methods include likelihood
ratio tests (LRTs), Akaike's Information Criterion (AIC), and score 
tests. Comparing two nested models via either LRTs or AIC means 
 using maximised log-likelihood values to assess the quality of
each model, while applying a penalty for model
complexity. For instance, conducting a LRT requires twice the difference of each
model's maximised log-likelihood. This is 
compared to a null distribution, where
the degrees of freedom can be thought of as containing the penalty for
model complexity \parencite{vaartAsymptoticStatistics2012}. 
Using AIC involves directly adding a penalty term to each
maximised log-likelihood and comparing the results \parencite{burnham2002}. 

To compare two nested models using a score test, a score test
statistic is calculated and compared to the same null
distribution as the LRT statistic. However, unlike the LRT, the score
test does not require both models to be fitted, but instead
employs gradients to inform the improvement in fit available by
perturbing the parameter estimates from the simpler model
\parencite{rao2005}. Specifically, the score test statistic is
calculated using the
score vector and information matrix for the more complicated model,
evaluated at the parameter estimates of the simpler model. See
\textcite{rao2005} for an overview and recent developments of score
tests. 

Each method has advantages and
disadvantages. It is straightforward to perform the
calculations required to compare models via LRTs or AIC. However, both
models must be fitted. To conduct score tests, only the simpler of the
two models needs to be fitted \parencite{rao2005}. However, the required
gradients can be challenging to obtain, often acting as a
deterrent for conducting these tests. 

Historically, LRTs and AIC have been the most widely-used methods
of model selection. However, when the models under comparison require
considerable time and resources to fit, these methods of comparison can
become inconvenient. This issue is amplified when considering
multiple potential models for a data set, as every model in the
candidate set must be fitted, potentially rendering comprehensive
model selection impractical.

In such cases, turning to score tests may be
beneficial, but for usability, we require a straightforward way of
calculating the gradients needed for the score test
statistic. Traditionally, options for obtaining these gradients
included symbolic differentiation and numeric approximations. The former
can quickly become unrealistic as model complexity grows, while the
latter often produces unstable and inaccurate
results \parencite{ADMBpaper}. However, a third option that has not so
far been widely used is automatic differentiation (AD). In this paper,
we demonstrate that recent AD tools developed for optimisation can be
repurposed to provide a newly straightforward implementation of score tests. 

By algorithmically harnessing the chain rule of differentiation, AD tools produce exact
gradients of any given function \parencite{ADMBpaper}. Using these
tools only requires the practitioner to supply the objective function 
\parencite{TMBpaper}. For statistical models, these tools are 
mainly used for model-fitting or calculating 
the Laplace approximation. We show that utilising the side-effects of these
capabilities -- repurposing their ability to calculate
exact log-likelihood gradients -- facilitates a
convenient implementation of score tests.

This has implications for a wide range of models. For instance, for
random effects models, intractable
likelihoods involving potentially high 
dimensional integrals produce gradients with no closed form, resulting in
a host of challenges. Model fitting previously required tailored
software, even for relatively simple examples such as
generalised linear mixed models (GLMMs), although gradients remained
elusive \parencite{lme4-pkg}. \textcite{wang2022} present extensive derivations
to obtain GLMM gradients, enabling previously
inaccessible tasks such as score tests.
However, there remain many random effects 
models for which such solutions and derivations don't exist. Random effects
models are just one illustration of the  
advancements offered by AD. 

To illustrate the benefits of score tests 
and AD tools, we will focus on the class of random effects models known as
spatial capture-recapture (SCR) models, used for estimating density
and distribution of animal populations. Model selection is often difficult 
for SCR models. Likelihood equations for these models involve products of two-dimensional spatial
integrals \parencite{borchersEfford2008}. This makes model-fitting, and therefore
using AIC to compare models, time consuming and resource intensive,
especially when many models are being considered. However, an absence
of suitable alternatives means comparison via AIC is the
primary method of SCR model selection
(e.g. \cite{secr-aic-ex}). Removing the barriers to conducting score
tests  presents a new and potentially impactful solution to this issue. 

We will explore the gains of conducting score tests using AD tools
via the framework introduced by \textcite{catchpoleMorgan1996}, and
later developed by \textcite{mccreaMorgan2011}. Following the
convention of organising the
candidate set into a tree structure, the framework provides an efficient way of
navigating through the set using score tests, 
amplifying the benefits of score tests for model selection. The issue
of choosing a model from a   
potentially vast candidate set of models of varying complexity is reduced to the
task of calculating score statistics and fitting a few, potentially
relatively simple, models. 

We present our findings using an SCR
data set of ovenbird captures \parencite{secr-pkg} with many potential
models to consider. We employ
the \lstinline{RTMB} R package \parencite{rtmb-pkg}, a widely-used AD tool, to
implement score tests for SCR model selection. Compared to traditional
methods, we examine how our approach enhances the 
convenience and accessibility of model selection, while accommodating
potentially common issues such as non-nested models.

Our proposed model selection approach is widely applicable, enabling
accessible model selection for many classes of models across a vast
range of fields.  

\section{Model selection using score tests}
\label{sec:model-sel}

\subsection{Calculating score test statistics}
\label{sec:calculating-score-stat}

We begin with a brief overview of score tests, and refer to texts such
as \textcite{rao2005} for more detailed information. When
comparing two nested models 
using a score test, the null hypothesis 
states the simpler model is adequate. We refer to this
 model as the `$H_0$ model', and the more complex model as the `$H_1$
model', with parameter vectors $\thetaZero$ and
$\thetaOne$, respectively. We define $\thetaOneTilde$ as the
$\thetaOne$ vector that shares the constraints of the $H_0$ model and
uses the estimates of $\thetaZeroMLE$. By definition, this vector
makes the $H_1$ model equivalent to the fitted $H_0$
model; see below for an example. Lastly, we refer to $\boldsymbol{u}_1
(\boldsymbol{\theta})$ as the gradient 
vector, also called the score, of the $H_1$ log-likelihood at
$\boldsymbol{\theta}$.

For a simple example, consider $Y_i \sim
\text{Normal}(\mu_i, 1)$, where $H_0: \mu_i = \alpha$ and $H_1: \mu_i
= \alpha + \beta x_i$ for some covariate $\boldsymbol{x}$, with
maximum likelihood estimate (MLE) vectors $\thetaZeroMLE$ and
$\thetaOneMLE$, respectively. For this model, $\thetaZeroMLE =
\hat{\alpha}$, and therefore 
$\thetaOneTilde = (\hat{\alpha}, 0)$, as under the $H_0$
model, $\beta=0$.

Briefly, score tests are $z$-tests summarising how `surprising'
$\scoreU$ is under $H_0$. Under $H_0$, the
expectation of this gradient is 
$0$. That is, $E[\boldsymbol{u}_1
(\thetaOneTilde^*)]=0$, where $\thetaOneTilde^*$ is the true
vector under $H_0$, as shown in Section \ref{sec:exp-zero-proof} of
the Appendix.

Using one dimension for illustration, the $z$-test
statistic is therefore
% $$
% \left.
%   \{\scoreUOneDim - E[u_1(\thetaOneTildeOD^*)]\}
%   \middle/
%   \sqrt{\text{Var}\{\scoreUOneDim}\}
% \right. =
% \left.
%   \scoreUOneDim \middle/ \sqrt{\text{Var}\{\scoreUOneDim}\}
% \right.
% $$
$$
\frac{\scoreUOneDim - E[u_1(\thetaOneTildeOD^*)]}
{\sqrt{\text{Var}\{\scoreUOneDim}\}}  =
\frac{\scoreUOneDim}{\sqrt{\text{Var}\{\scoreUOneDim}\}}.
$$

It can be shown that $\text{Var}\{\scoreUOneDim\} =
\mathcal{I}_1(\thetaOneTildeOD)$, where $\mathcal{I}_1(\thetaOneTildeOD)$ is
the $H_1$ information matrix evaluated
at $\thetaOneTildeOD$. Refer to Section
\ref{sec:var-info-equiv-proof} of the Appendix for the algebraic
justification. Therefore, the z-statistic can also be expressed as
$\scoreUOneDim / \{\mathcal{I}_1(\thetaOneTildeOD)\}^{1/2}$, which can be interpreted
geometrically as taking into account the
overall shape of the likelihood to
assess the magnitude of $\scoreUOneDim$. 

Figure \ref{fig:fig1a} illustrates a one-dimensional scenario with
$Y_i \sim \text{Normal}(\mu, 1)$, where
$H_0: \mu = 0$ and $H_1: \mu = \alpha$, and two log-likelihood curves
with different sample sizes that share $\scoreUOneDim=5$. Both the
score test and LRT statistics indicate no evidence against the
$H_0$ model for the data of the orange curve, but that $H_0$ is
inadequate for the data of the blue 
curve. As $\scoreUOneDim$ is a sum of independent contributions, the
z-statistic is asymptotically Normal$(0,1)$ under $H_0$, so a two-tailed 
$p$-value may be obtained to assess $H_0$. However, following convention, the score test
statistic is found by squaring this standardised value,
i.e., $S=\scoreUOneDim^2/\mathcal{I}_1(\thetaOneTildeOD)$,
resulting in an asymptotic chi-squared distribution.

The above reasoning holds as model dimension increases. Therefore, in general,
$S = \scoreU^T \scoreI^{-1}
\scoreU$, where $\scoreU$ is the score vector of the $H_1$ model
evaluated at $\thetaOneTilde$ and $\scoreI$ is the information
matrix. For practical calculations, we will focus on using the observed information
matrix; see the Discussion for further details and alternative options.

% -- as in z-test, rely on asymptotic Normality to produce a p-value
% assessing model fit 
% -- standardise = score/sqrt(information)
% -- and by the nature of the score (sum of independent contributions to
% log-likelihood), this is asymptotically Normal
% -- squaring means that the test becomes two-sided. Also gives same
% chi-sq distribution as LRT, for example

As shown in one dimension in Figure \ref{fig:fig1b}, $S$ is equivalent
to a LRT statistic if a quadratic approximation is used for the log-likelihood.
This is derived in one dimension in Section \ref{sec:lrt-s-equiv-proof} of
the Appendix. Therefore, when the log-likelihood is quadratic, the LRT
statistic and score statistic are exactly equal, and otherwise they are
asymptotically equal. The null distribution for both score tests and
LRTs is a chi-squared
distribution where the degrees of freedom is the difference in the
number of parameters for the $H_0$ and $H_1$ model.

\begin{figure}[h]
  \captionsetup[subfigure]{labelformat=empty}
  \centering
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{Figures/Fig1a.pdf}
    \caption{}
    \label{fig:fig1a}
  \end{subfigure}
  \begin{subfigure}{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{Figures/Fig1b.pdf}
    \caption{}
    \label{fig:fig1b}
  \end{subfigure}
  \caption{(a) Log-likelihood curves under $H_1$ for samples of different
    sizes, where $Y_i \sim \text{Normal}(\mu, 1)$, with $H_0: \mu = 
    0$ and $H_1: \mu = \alpha$. The log-likelihood
    curves have been shifted for interpretability, as indicated on the y-axes. (b) The blue curve
    represents a non-parabolic $H_1$ log-likelihood, and the dashed
    pink line is the second-order Taylor series expansion of this
    log-likelihood about $\thetaOneTildeOD$. 
    `LRT' and `S' represent the LRT and score statistic,
    respectively.}   
  \label{fig:fig1}
\end{figure}

Previously, obtaining the score vector, $\boldsymbol{u}_1 (\thetaOneTilde)$, and information
matrix, $\mathcal{I}_1 (\thetaOneTilde)$, posed an obstacle to
implementing score tests. However, following recent software advancements, we
can readily perform these calculations using the \lstinline{RTMB} R package,
a commonly-used AD tool. By writing the $H_1$ log-likelihood as 
an R function, it is straightforward to extract the score vector
and information matrix. Furthermore, using AD to also fit
the $H_0$ model can provide time savings, especially for sophisticated models.

An illustration of using \lstinline{RTMB} versus symbolic differentiation
is given in Section \ref{sec:score-test-rtmb-ex}, where $S$ is found
for the example with $Y_i \sim 
\text{Normal}(\mu_i, 1)$, where $H_0: \mu_i = \alpha$ and $H_1: \mu_i
= \alpha + \beta x_i$ for some covariate $\boldsymbol{x}$. Although these
models are particularly simple,
obtaining $\scoreU$ and $\scoreI$ is surprisingly
involved. The symbolic differentiation required to calculate $S$
increases considerably as model complexity increases, typically
becoming impractical. 

\subsection{Implementing level-based model selection}
\label{sec:implementing-framework}

The key advantage of model selection via score tests is that not all 
models under consideration need to be fitted. However, we require a
principled way to decide which models to fit. One way to do this is by
organising the candidate set into a tree 
structure to perform level-based model selection, as proposed by
\textcite{catchpoleMorgan1996}. Through the construction of levels,
this framework defines an efficient path to the final selected model,
beginning by considering the simplest models. Score tests are used to
select the best model at each level. 

% The approach 
% defines an efficient path to the final selected model, beginning by
% considering the simplest models. This is done through the construction
% of `levels', and the use of score tests to select the best model at
% each level. 

As described in \textcite{mccreaMorgan2011}, 
the most basic model defines Level 0 of the candidate set, and Level 1
contains all models with one added model term. For instance, in the
example with $Y_i \sim \text{Normal}(\mu_i, 1)$ where $H_0: \mu_i =
\alpha$ and $H_1: \mu_i = \alpha + \beta x_i$, if $H_0$ is the Level 0
model, then $H_1$ is a Level 1 model. Score tests are used to compare
all Level 1  models to the Level 0 model. As in 
Section \ref{sec:calculating-score-stat}, we refer to the Level 0
model as the `$H_0$ model', and the Level 1 models as `$H_1$
models'. The $H_1$ model associated with the smallest significant
$p$-value defines the new null hypothesis -- that is, it becomes the new
$H_0$ model. Level 2 is then constructed, consisting of all models with one
additional term relative to this model, and the
process is repeated. We stop at the
level where all of the score tests produce $p$-values 
that are large enough for the analyst to be satisfied with the $H_0$
model. At this point, the $H_0$ model becomes the chosen model.

Although this tree structure can be generalized beyond score tests -- AIC values
or LRT statistics could be used instead, for example
-- it allows for especially convenient model selection when using
score tests because only the $H_0$ models need to be fitted. The
stepwise nature of the method also promotes 
convenient model-fitting -- the MLEs from a given $H_0$ model may
provide appropriate starting values for the next $H_0$ model
\parencite{mccreaMorgan2011}.

Combining the benefits of level-based
score test model selection with the convenience of AD, we require
fewer resources and less time than traditional model selection
techniques, especially for large candidate sets. Fitting all models
under consideration is replaced by accessible calculations and fitting
a few models.   

\subsection{Non-nested models, boundary considerations and unidentifiability}
\label{sec:non-nested-and-boundary-consid}

When models are non-nested, the $H_0$ model cannot be obtained from
the $H_1$ model. No $\thetaOneTilde$ vector exists, so a score test
statistic cannot be calculated. 

Problems also arise when the regularity conditions underlying score
tests do not hold. This may occur because 
$\thetaOneTilde$ lies on the boundary of the parameter space or is
unidentifiable under the $H_0$ model. The chi-squared distribution no
longer holds for the null distribution, so the distribution must be
derived explicitly or via simulation. For example,
\textcite{andrews-2001} derives bespoke null distributions for
selected models.  

Specific elements of $\scoreU$ being equal to zero can also 
produce identically zero and therefore uninformative score
statistics. For further information, refer to texts such as
\textcite{cox-hinkley-1979} and \textcite{lee-chesher-1986}. Briefly,
identically zero statistics arise when all parameters distinguishing
the $H_1$ and $H_0$ models have first derivatives equal to zero. For
example, consider a $H_0$ non-mixture model with likelihood
$f(\boldsymbol{y}|\theta)$ that is nested under a $H_1$ two-component mixture model
with likelihood $(1-\pi)f(\boldsymbol{y}|\theta_A) + \pi
f(\boldsymbol{y}|\theta_B)$. Under $H_1$, observations can fall into
group A or B, with probability $(1-\pi)$ and $\pi$ respectively, whereas
under $H_0$ all observations are in the same group. Parameters $\pi$
and $\theta_B$ distinguish both models, and no matter how
$\thetaOneTilde$ is constructed to  align with $H_0$ constraints
(e.g. setting $\theta_A=\theta_B$ or $\pi=0$), $\scoreU$ in the
direction of these parameters will always be $0$. As the score test
relies on these derivatives, the score statistic is identically zero. 

When such issues arise, we propose separating the candidate set into
groups -- where models in each group fall into a nested structure with
no boundary, identifiability or gradient problems -- and carrying out
level-based model selection on each  group. We then use AIC to make a
final selection from the chosen models in each group. See Section
\ref{sec:ovenbirds} for an example. Using AIC like this is unlikely to
require any extra models be fitted, as it is only if we conduct score
tests to the end of a group's tree (due to significant $p$-values
occurring at each level) that
the final chosen model will not already be fitted as an $H_0$ model.

\section{Application: ovenbirds}
\label{sec:ovenbirds}

\subsection{Data set}
\label{sec:data-set}

\textcite{borchersEfford2008} illustrate SCR model selection using AIC. The data
set consists of captures of 
red-eyed vireos from a mist-netting survey on the Patuxent Research Refuge 
in Maryland, USA, conducted in the 1960s. Considering
a range of modeling choices, the authors examine a candidate set of 
fifteen potential models, and select the model with the lowest
AIC as the final model.

Working with a similar data set allows consideration of the same
modeling choices for comparative purposes. The `ovenbird' data set, 
available via the `secr' R package \parencite{secr-pkg}, contains adult captures from a 
mist-netting survey of ovenbirds conducted from 2005 to 2009 at the Patuxent
Research Refuge. The forest in the area has
changed little since the previous survey in the 1960s. Therefore, to
illustrate model selection, we employ the same model terms as 
\textcite{borchersEfford2008}. As described below, each model
estimates three parameters, $D$, $g_0$ and $\sigma$.
\textcite{borchersEfford2008} employ five distinct
modeling choices for $g_0$ and three for $D$ and $\sigma$, and considering all possible
combinations of these yields our
candidate set of 45 models. Harnessing the convenience of score tests
and AD via level-based model selection, it becomes practically
feasible to consider a candidate set beyond the original 15 models
considered.  

% afforded by AD and score tests,
% it becomes practically feasible to consider a candidate set beyond the original
% 15 models considered. 

A total of 44 mist nets were passively operated for nine or
ten non-consecutive days in late May and June of each year. We retain any 
records of individual birds being captured more than once in a
day. Below, we compare our score test approach to the traditional
approach of model selection using AIC. 

% To construct an accurate picture of the benefits afforded by model
% selection via score tests and AD tools, alongside our proposed
% approach, we implement model selection via
% AIC. Using an extensive candidate
% set, we conduct a comprehensive comparison of a novel, more efficient,
% method of model selection with a traditional approach.

\subsection{Implementing level-based model selection}
\label{sec:implementing-for-ex}

% Simply put, the likelihood of our relatively basic SCR models can be
% written as follows: 
% \begin{equation}
%   L = \frac{e^{-E[n]}}{n!} \prod_{i=1}^n \int_{\mathcal{R}^2} \mathbb{P} \left(
%     \begin{tabular}{ c }
%       capture history \\
%       i \\
%     \end{tabular}
%     \middle| g_0, \sigma, \boldsymbol{s} \;
%   \right)
%   \left(
%     \begin{tabular}{ c }
%       intensity function \\
%       at \boldsymbol{s} \\
%     \end{tabular}
%     \middle| \boldsymbol{\phi} \;
%   \right)
%   % \mathbb{P} \begin{pmatrix} \text{capture history } i, \text{given }\\
%   %   \text{the animal is at } \boldsymbol{s} \end{pmatrix}
%   % \times \begin{pmatrix} \text{expected number of animals} \\ \text{per
%   %     unit area at } \boldsymbol{s} \end{pmatrix}
%   d \boldsymbol{s}
% \end{equation}

As described in Section \ref{sec:overview-candidate-models} of the
Appendix and in texts such as \textcite{secr-book}, specifying an SCR
model involves selecting a spatial point process to describe
animal distribution in the area of interest, and a detection function
to describe the sampling process. As discussed in Section
\ref{sec:overview-candidate-models} of the Appendix, we select a
homogeneous Poisson point process and a half-normal detection
function for our models, following the example of
\textcite{borchersEfford2008}. In practice, making accurate selections
enhances the validity of inference. Specifically, misspecification of
the detection function is known to produce misleading results,
emphasising the importance of reliable model selection techniques.  

However, SCR likelihoods are generally elaborate, making model selection
potentially fraught. For example, 
Equation \ref{eq:scr-lik} represents a relatively basic simplified SCR
likelihood as described in \textcite{borchersEfford2008}. We omit
explanations of the terms in this equation for simplicity, refer to
the text for more details. This expression illustrates how even
relatively simple 
SCR models are sophisticated statistical models, requiring evaluation of a product of 
two-dimensional integrals. It is used to calculate likelihood values for the SCR
models we consider, and as we see below, its complexity makes
traditional model selection techniques time consuming. Selecting a
different spatial point process would increase the complexity of the
likelihood further, escalating barriers for model selection.

\begin{equation} \label{eq:scr-lik}
L = \frac{\{Da(\boldsymbol{\theta})\}^n \exp
  \{-Da(\boldsymbol{\theta})\}}{n!} \times \binom{n}{n_1,...,n_c}
\prod_{i=1}^n \frac{\int_{\mathbb{R}^2}
  \mathbb{P}(\boldsymbol{\omega}_i | \boldsymbol{X}; \boldsymbol{\theta}) d
  \boldsymbol{X}}{a(\boldsymbol{\theta})} 
\end{equation}

% SCR likelihoods are generally elaborate, making
% tasks such as model selection potentially fraught.

% Specifically, 
% evaluating an SCR likelihood requires a product of two-dimensional
% integrals, where each integral is an infinite sum over the model's
% random effects. For example, a standard SCR likelihood as described in
% \parencite{borchersEfford2008} is given in Equation \ref{eq:scr-lik}
% below. We omit explanations of the terms involved for simplicity,
% refer to this text for more details. 

% \begin{equation} \label{eq:scr-lik}
%   L = \frac{\exp \lrbr{- \int_{\mathcal{R}^2} D(\boldsymbol{X}' ;
%       \boldsymbol{\phi}) p_{.} (\boldsymbol{X}' ; \boldsymbol{\theta})
%       d \boldsymbol{X}'}}{n!} \prod_{i=1}^n \int_{\mathcal{R}^2}
%   \mathbb{P}(\boldsymbol{\omega}_i ; \boldsymbol{\theta},
%   \boldsymbol{X}) D(\boldsymbol{X} ; \boldsymbol{\phi}) d \boldsymbol{X}
% \end{equation}

Specifically, due to the complexity of the likelihoods involved,
fitting every model in a comprehensive candidate set  
such as ours requires significant resources and time. Despite this,
the convenience of calculating AIC means it is often used for
SCR model selection \parencite{efford-schofield}, often making model
selection laborious and potentially impractical. Although popular models
for estimating animal abundance, the complexity of SCR likelihoods
means derivatives and therefore tools such as score tests were
previously elusive. % The benefits of simplifying model selection via
% our proposed approach are substantial, allowing newly accessible
% model selection for these models.

Please refer to Section \ref{sec:overview-candidate-models} of the Appendix for an
overview of the models in our candidate set. Briefly, as we use a
homogeneous Poisson point process and half-normal detection function, 
our parameters of interest are: $D$, $g_0$ and $\sigma$. We
consider a range of modeling choices for these parameters, and
considering all possible combinations of the resulting model terms
yields the forty five models in our candidate set. See Table
\ref{table:aic-table} in Section \ref{sec:aic-res-app} of the Appendix
for the complete list of models. 

Due to the comprehensive nature of the candidate set, we encounter
potentially identically zero score statistics, as described in Section
\ref{sec:non-nested-and-boundary-consid}. Specifically, we consider models with a
two-component finite mixture for $g_0$ and/or $\sigma$. To avoid
these issues, we only compare models with matching mixture
terms for $g_0$ and $\sigma$. Therefore, we construct four
groups for model selection: models with no mixture terms, models with
mixture terms for $g_0$ only, models with mixture terms for
$\sigma$ only, and models with mixture terms for $g_0$ and
$\sigma$. If we were also considering models with different detection
functions -- i.e. non-nested models, as mentioned in Section
\ref{sec:non-nested-and-boundary-consid} -- we would separate
models further by detection function. 

To conduct model selection, we apply the level-based framework of
\textcite{catchpoleMorgan1996} to each group, using score tests to
compare models. We write the objective function
(the negative log-likelihood) for every candidate model as an R 
function and follow the approach outlined in Section \ref{sec:rtmb-appendix}  
of the Appendix, using \lstinline{RTMB} to fit the $H_0$ model and
obtain the required gradients. We stop at the level of the framework  where $p$-values
are no longer significant  according to the commonly-used 5\% threshold. Using AIC, we
compare the chosen model from each group, selecting the model
with the smallest AIC as the final model. 

For a thorough examination of the benefits of this approach, we also
implement model selection according to standard
practices. Specifically, we fit every model using
\lstinline{secr.fit()}, and compare them using AIC values obtained via
\lstinline{AIC()} (both functions are available via the
\lstinline{secr} package). Furthermore, to enhance the accuracy of comparison,
we consider our score test approach with a significance threshold of
16\%, chosen based on the findings of \textcite{sutherland2023}. Lastly, we
implement the level-based framework using AIC values in place of score
tests, completing a comprehensive assessment of our proposed approach. 

\subsection{Results}
\label{sec:ex-results}

Implementing model selection using score tests and a significance
threshold of 5\%, selecting a final model takes approximately 13.4
minutes. In contrast, implementing model selection via the
\lstinline{secr} package and AIC takes approximately 24.8
hours. Both approaches select the same final model -- a model with constant $D$ and
$\sigma$ for all individuals across the survey years, and a behaviour
response for $g_0$ -- confirming the reliability of the proposed 
approach.

As described in \textcite{sutherland2023}, when two models differ by
one term, using AIC for comparison is equivalent to conducting a LRT
with a significance level of 0.157. The two approaches select
the same model. By the asymptotic equivalence between LRTs and score
tests, using AIC is therefore asymptotically
equivalent to a score test with a $p$-value threshold of
0.157. Thus, to ensure the accuracy of the above comparison, we can
implement our score tests using this threshold, although as shown in Table
\ref{table:score-table}, the results remain unchanged. 

% \textit{
% \textbf{Confirm with Ben and Rachel -- bottom of p4 in Sutherland
%   paper, why shouldn't lambda be at least 0? Wouldn't that give
%   delta-AIC < 2, instead of lambda at least 2?}
% }

The significant time savings we see can be attributed to the reduced number
of models to fit, and the use of AD. By using the level-based
framework of \textcite{catchpoleMorgan1996} for each constructed
group, only 6 of the 45 candidate models need to be
fitted. Furthermore, using \lstinline{RTMB} significantly speeds 
up model fitting and calculating score statistics, as
potentially impractical iterative procedures are 
replaced with accessible calculations using conveniently-obtained
exact gradients. For instance, if we run the level-based framework using AIC values in
place of score tests and use \lstinline{RTMB} to fit all models, we
see similar time savings. However, the time taken remains greater than
when using score tests. As model complexity and quantity increases, the time
savings provided by score tests -- i.e. the time savings from only
needing to fit one model in each pair -- will also increase. As can be seen
in Tables \ref{table:score-table} and \ref{table:aic-vals-score-tests}, when using AIC, 
the choice of final model remains 
unchanged, further confirming the validity of our results. Note that we also
ran the level-based framework on the four groups described in Section
\ref{sec:implementing-for-ex} to reduce the effect of AIC favouring
larger mixture models, occurring due to a violation of regularity conditions
with mixture models \parencite{mcL-Peel2000}.    

In Tables \ref{table:score-table} and \ref{table:aic-vals-score-tests}, we provide results
from using score tests for model selection for each group, stopping
where $p$-values are no longer significant based on a 5\%  
threshold. In Table \ref{table:score-table}, the model selected from
each group is displayed in bold. The close alignment between score
statistics, LRT statistics and AIC values confirm the accuracy of the
score tests. Due to negligible differences, we only report score test
$p$-values (and not LRT $p$-values). In Table
\ref{table:aic-vals-score-tests}, we show AIC values for the model
selected from each group, leading to our final choice. From these AIC
values, we can confirm the potential issue of AIC favouring mixture models does not
affect our final choice. All results in these tables were found
using \lstinline{RTMB} for model-fitting and obtaining
derivatives. Due to relevance, the time to conduct the LRTs and obtain AIC
values via \lstinline{RTMB} are excluded from the time taken
above.

For interest, in Table \ref{table:aic-table} from Section
\ref{sec:aic-res-app} of the Appendix we provide
results from using AIC values, calculated for every model in the
candidate set via the \lstinline{secr} package. Simple
derivations can produce the AIC and $\Delta$AIC values we see in 
Tables \ref{table:score-table} and
\ref{table:aic-vals-score-tests}. The resulting values align closely,
further confirming the validity of our 
\lstinline{RTMB} results. As in Section
\ref{sec:overview-candidate-models} 
of the Appendix, the model descriptions in both tables are provided in
the parlance of the \lstinline{secr} package, refer to this section
for more details.

% \textbf{Confirm equivalence of delta-AIC values :)}

% \textbf{(Doesn't seem worthwhile containing delta-AIC values... if is
%   relative to final model, are just repeating results from the AIC
%   table? Is more a confirmation that our objective values are correct?)}


\begin{table}[h!]
  \caption{Results from the score test approach for the four
    constructed groups. We provide: the level each model 
    falls into within the score test framework (`Level'), the model
    description in \lstinline{secr} parlance (`Model'), the number of
    model parameters (`npar'), the score statistic (`$S$'), the LRT
    statistic (`$LRT$') and the score test $p$-value (`$p$-value'). We
    also supply the 
    difference in AIC for all $H_1$ models, relative to the $H_0$ model
    from the previous level (`$\Delta$AIC'). The selected
    model from each group is shown in bold. All values are reported to
    1 dp (or 1 sf, where appropriate).}  
  \label{table:score-table}
  \centering
  \begin{tabularx}{0.8\textwidth} { 
    >{\centering\arraybackslash \hsize=0.3\hsize}X 
    >{\centering\arraybackslash \hsize=4\hsize}X 
    >{\centering\arraybackslash \hsize=0.5\hsize}X
    >{\centering\arraybackslash \hsize=0.5\hsize}X
    >{\centering\arraybackslash \hsize=0.5\hsize}X
    >{\centering\arraybackslash \hsize=0.7\hsize}X
    >{\centering\arraybackslash \hsize=0.5\hsize}X
    }
    \hline
    \multicolumn{7}{ c }{Group 1} \\ 
    \hline
    \hline
    Level & Model & npar & $S$ & $LRT$ & $p$-value & $\Delta$AIC \\
    \hline
    0 & \Dconst, \gconst, \sigmaconst & 3 & - & - & - & - \\
    \hdashline
    1 & \textbf{\Dconst}, $\boldsymbol{g_0\sim \textbf{b}}$,
            $\boldsymbol{\sigma\sim 1}$ & $\boldsymbol{4}$ &
                                                             $\boldsymbol{10.8}$
                               & $\boldsymbol{8.4}$ &
                                                      $\boldsymbol{0.001}$
                                                 &
                                                   $\boldsymbol{-6.4}$    \\
          & \DSession, \gconst, \sigmaconst & 4 & 1.1 & 1.1 & 0.3 & 0.9 \\
          & \Dsession, \gconst, \sigmaconst & 7 & 3.2 & 3.2 & 0.5 & 4.8 \\
          & \Dconst, \gsession, \sigmaconst & 7 & 4.8 & 4.7 & 0.3 & 3.3 \\
          & \Dconst, \gconst, \sigmasession & 7 & 5.2 & 5.0 & 0.3 & 3
    \\
    \hdashline
    2 & \DSession, \gbehav, \sigmaconst & 5 & 1.0 & 1.0 & 0.3 & 1.0 \\
          & \Dsession, \gbehav, \sigmaconst & 8 & 3.2 & 3.2 & 0.5 & 4.8 \\
          & \Dconst, \gsessionbehav, \sigmaconst & 8 & 5.6 & 5.3 & 0.2
                                                 & 2.7 \\
          & \Dconst, \gbehav, \sigmasession & 8 & 6.3 & 6.0 & 0.2 & 2.0
    \\
    \hline
    \multicolumn{7}{ c }{Group 2} \\ 
    \hline
    \hline
    Level & Model & npar & $S$ & $LRT$ & $p$-value & $\Delta$AIC \\
    \hline
    0 & \textbf{\Dconst}, $\boldsymbol{g_0\sim \textbf{h2}}$,
        $\boldsymbol{\sigma\sim 1}$ & $\boldsymbol{5}$ & \textbf{-} &
                                                                      \textbf{-}
                                       & \textbf{-} &
                                                      \textbf{-}
    \\
    \hdashline
    1 & \DSession, \gmix, \sigmaconst & 6 & 1.1 & 1.1 & 0.3 & 0.9 \\
          & \Dsession, \gmix, \sigmaconst & 9 & 3.2 & 3.2 & 0.5 & 4.8 \\
          & \Dconst, \gmix, \sigmasession & 9 & 4.2 & 4.2 & 0.4 & 3.8 \\
    \hline
    \multicolumn{7}{ c }{Group 3} \\ 
    \hline
    \hline
    Level & Model & npar & $S$ & $LRT$ & $p$-value & $\Delta$AIC \\
    \hline
    0 & \Dconst, \gconst, \sigmamix & 5 & - & - & - & \\
    \hdashline
    1 & \textbf{\Dconst}, $\boldsymbol{g_0\sim \textbf{b}}$,
        $\boldsymbol{\sigma\sim \textbf{h2}}$ & $\boldsymbol{6}$ &
                                                                   $\boldsymbol{11.4}$
                               & $\boldsymbol{8.7}$ &
                                                      $\boldsymbol{0.0007}$
                                                 &
                                                   $\boldsymbol{-6.7}$  \\
          & \DSession, \gconst, \sigmamix & 6 & 1.1 & 1.1 & 0.3 & 0.9 \\
          & \Dsession, \gconst, \sigmamix & 9 & 3.2 & 3.2 & 0.5 & 4.8 \\
          & \Dconst, \gsession, \sigmamix & 9 & 4.3 & 4.5 & 0.4 & 3.5 \\
    \hdashline
    2 & \DSession, \gbehav, \sigmamix & 7 & 1.0 & 1.0 & 0.3 & 1.0 \\
          & \Dsession, \gbehav, \sigmamix & 10 & 3.2 & 3.2 & 0.5 & 4.8
    \\
          & \Dconst, \gsessionbehav, \sigmamix & 10 & 4.4 & 4.6 & 0.4
                                                 & 3.4
    \\
    \hline
    \multicolumn{7}{ c }{Group 4} \\ 
    \hline
    \hline
    Level & Model & npar & $S$ & $LRT$ & $p$-value & $\Delta$AIC \\
    \hline
    0 & \textbf{\Dconst}, $\boldsymbol{g_0\sim \textbf{h2}}$,
        $\boldsymbol{\sigma\sim \textbf{h2}}$ & $\boldsymbol{6}$ &
                                                                   \textbf{-}
                               & \textbf{-} & \textbf{-} &
                                                            \textbf{-} 
    \\
    \hdashline
    1 & \DSession, \gmix, \sigmamix & 7 & 1.1 & 1.1 & 0.3 & 0.9 \\
          & \Dsession, \gmix, \sigmamix & 10 & 3.2 & 3.2 & 0.5 & 4.8 \\
  \end{tabularx}
\end{table}

\begin{table}
  \centering
  \begin{tabular} { | c | c | c | }
    \hline
    Group & Model & AIC \\
    \hline
    1 & \Dconst, \gbehav,
        \sigmaconst & 1726.8 \\
    2 & \Dconst, \gmix, \sigmaconst & 1733.9 \\
    3 & \Dconst, \gbehav, \sigmamix & 1728.1 \\
    4 & \Dconst, \gmix, \sigmamix & 1736.2 \\
    \hline
  \end{tabular}
  \caption{AIC values for the model selected from each group using the
    level-based framework with score tests.}
  \label{table:aic-vals-score-tests}
\end{table}

\section{Discussion}
\label{sec:discussion}

We propose and outline the use of AD via \lstinline{RTMB} to implement
score tests for more convenient and accessible model selection when
compared to traditional, widely-used model selection techniques such
as AIC values and LRTs. Additionally, we recommend the level-based model
selection framework of \textcite{catchpoleMorgan1996} to amplify these
benefits. When this framework is used in conjunction with score tests,
only a few models from the candidate set typically need to be fitted
to select a final model. We also extend the framework to
accommodate potential issues when comparing models via score tests. We
focus on applying our new approach to SCR models, using the `ovenbird' data set
available via the \lstinline{secr} R package, and illustrate the significant time
savings afforded by the approach. Traditional model selection
techniques are often impractical for models of similar complexity, due
to the time and resources required to fit all models in the candidate set.

\subsection{General considerations}
\label{sec:disc-general}

Generally, when performing model selection, it is necessary to take model
uncertainty into account, e.g. via a bootstrap simulation, so standard
errors are not underestimated \textbf{(Claeskens and Hart
  2008?)}. Furthermore, texts such as \textcite{mccreaMorgan2011} 
recommend using simulation to select an appropriate conservative
level, mitigating against an inflated false positive rate due to multiple
tests. Refer to the text for more details. Lastly, with hierarchical
model selection it is generally 
possible to lose important model features. Therefore,
\textcite{mccreaMorgan2011} suggest the incorporation of step-down
tests, such as LRTs, in conjunction with score tests for model
selection.

\subsection{Applicability}
\label{sec:disc-applicability}

Our proposed model selection approach can be
applied to a wide range of models, the only requirements are those
underlying score tests. Therefore, applicability only requires
standard regularity assumptions, similar to those   
underlying LRTs. However, unlike LRTs, the local nature of score tests
requires these assumptions only hold in a neighbourhood around
$\thetaOneTilde$, rather than globally across the $H_1$ parameter
space. For example, in Section
\ref{sec:non-nested-and-boundary-consid} we only consider boundary
estimates and unidentifiability at $\thetaOneTilde$. The applicability
of our approach is therefore similar to, if not wider than, that of the
commonly-used LRT. 

\subsection{Observed vs expected information matrix}
\label{sec:disc-obs-exp-mat}

It is possible to use the observed or expected information matrix to
calculate score statistics, both have the same asymptotic
distribution \parencite{catchpoleMorgan1996}. Due to the considerable
gain in convenience, we generally recommend using the observed
information matrix. As illustrated in Section \ref{sec:rtmb-appendix},
with our approach this requires only a few lines of code that are
quick to run. In contrast, the expected information matrix requires
potentially extensive symbolic calculations, if tractable. For
sophisticated models, intractability often means numerical integration or
Monte Carlo simulation is needed to obtain an estimate of the expected
information matrix. As mentioned in Section \ref{sec:introduction}, the former
can be unrealistic as model complexity grows, while the latter can
require extensive time and resources.

Texts such as \textcite{catchpoleMorgan1996} and
\textcite{mccreaMorgan2011} explore the benefits afforded by the
expected information matrix. However, with the advancement of AD, these
benefits are now outweighed by the convenience of the observed
information matrix. \textcite{catchpoleMorgan1996} report closer  
alignment between score and LRT statistics with the expected information
matrix. As shown in Section \ref{sec:ex-results}, this does not
exclude close alignment when using the observed information  
matrix. Using simulation, \textcite{mccreaMorgan2011} find improvement in 
model selection accuracy from the expected information
matrix. The reported effect is not large and decreases as sample size
increases. Furthermore, observed information score statistics found
using AD over numeric approximations are more accurate, potentially
reducing these benefits.  

% In \textcite{mccreaMorgan2011}, the authors mention that a
% by-product of using the expected information matrix is detecting
% unidentifiability. While this is likely useful for large candidate
% sets of sophisticated models, the presence of alternative (likely more
% convenient) methods for detecting unidentifiability
% means this is insufficient to recommend the expected
% information matrix.  *** Check with Ben and Rachel -- OK to
% exclude?? 
 
A scenario where we recommend considering the expected information
matrix is when negative score statistics arise. As
explored in \textcite{morgan2007}, the observed information
matrix may occasionally result in a negative statistic, as a poor
fit under $H_0$ produces an observed information matrix that is not
positive definite. In this case, alternatives include
using the expected information matrix or the LRT
statistic. Specifically, when dealing with sophisticated models where
the required calculations are intractable
(e.g. SCR models), we recommend Monte Carlo methods to estimate
the former. For our purposes, this
means simulating data under the fitted $H_0$ model, evaluating the
$H_1$ observed information matrix at the $H_0$ MLEs for each data set,
and then averaging across all matrices. However, due to the likely loss in
efficiency, using the LRT statistic may be more
convenient. The asymptotic equivalence of score and LRT
statistics means accuracy is likely to be preserved.

\subsection{Concluding remarks}
\label{sec:disc-conclusion}

Harnessing recent software advancements with AD and the local nature
of score tests, we present an approach that considerably improves the
convenience of comprehensive model selection,
applicable to a wide range of statistical models. Through the lens of
the level-based framework of \textcite{catchpoleMorgan1996}, only a small subset 
of the candidate set must be fitted, further enhancing the time
savings. As mentioned above, significance thresholds can be selected
to ensure equivalence to well-established methods of model selection,
such as AIC. 

Further work includes investigation of an alternative method to choose
the final model when running the level-based framework on multiple
groups, due to potential bias in AIC when considering mixture
models. Additionally, there is scope for simulation to confirm the absence of material
benefit when using the expected information matrix, and further
exploration of the implementation of considerations in Section \ref{sec:disc-general}.

\section{Appendix}
\label{sec:appendix}

\subsection{Score test proofs}
\label{sec:score-test-proofs}

For ease of explanation, in the following sections we work with
a one-dimensional $H_1$ parameter vector, $\thetaOneOD$, and a data set
consisting of one observation, $Y$. Therefore, $\llSimple$
represents the $H_1$ log-likelihood. We assume the required
regularity conditions for score tests hold. 

\subsubsection{$E[\boldsymbol{u}_1(\thetaOneTilde^*)]=0$}
\label{sec:exp-zero-proof}

The $H_1$ log-likelihood can be expressed as: $\llSimple
= \log \lrbr{\pdfSimple}$. Therefore, the score vector is:
$\scoreSimpleCond = \deltaThetaOneOD \log \lrbr{\pdfSimple} =
\frac{1}{\pdfSimple} \deltaThetaOneOD \lrbr{\pdfSimple}$.

Then, if $\thetaOneTildeODTrue$ represents the true parameter vector under
$H_0$, the expectation of $\scoreSimpleTrue$ -- an expectation
across data sets -- is given by:
\begin{equation}
  \begin{split}
    E_Y\lrsq{\scoreSimpleTrueCond} & = \intSimple \left. \lrsq{\frac{1}{\pdfSimple}
        \deltaThetaOneOD \lrbr{\pdfSimple}} \right|_{\thetaOneTildeODTrue}
    \pdfSimpleTrue dy \\ 
    & = \intSimple \left. \deltaThetaOneOD \lrbr{\pdfSimple}
    \right|_{\thetaOneTildeODTrue} dy = \left. \deltaThetaOneOD \lrbr{\intSimple
        \pdfSimple dy} \right|_{\thetaOneTildeODTrue} \\
  \end{split}
\end{equation}
as under the assumed regularity conditions, the order of
differentiation and integration can be swapped. 

And since $\intSimple \pdfSimple dy = 1$, we can therefore say
$E_Y[\scoreSimpleTrueCond]=0$ -- that is,
$E[\scoreSimpleTrue]=0$. Similar reasoning holds as the dimension of
the parameter space and data set increases, so in general,
$E[\boldsymbol{u}_1(\thetaOneTilde^*)]=0$. 

\subsubsection{$\text{Var}\lrbr{\scoreU} = \mathcal{I}(\thetaOneTilde)$}
\label{sec:var-info-equiv-proof}

Below, we will
use $\scoreSimple$ and $\scoreSimpleCond$ interchangeably, for
brevity.

Using reasoning similar to Section \ref{sec:exp-zero-proof}, it can be
shown $E_Y [\scoreSimple] = 0$, in general. Therefore,
$\text{Var}\lrbr{\scoreSimple} = E_Y[\scoreSimple^2]$. We 
can derive an expression for $E_Y[\scoreSimple^2]$, such that 
$\text{Var}\lrbr{\scoreSimple} = \mathcal{I}(\thetaOneOD)$.

Firstly, recall from Section \ref{sec:exp-zero-proof}:
$E_Y[\scoreSimple] = \intSimple \llSimpleFirstDeriv \pdfSimple
dy$. Therefore:
\begin{equation}
  \label{eq:exp-deriv}
  \begin{split}
  \deltaThetaOneOD E_Y[\scoreSimple] & = \intSimple \deltaThetaOneOD \llSimpleFirstDeriv \pdfSimple
  dy \\
  & = \intSimple \llSimpleSecondDeriv \pdfSimple + \lrbr{\llSimpleFirstDeriv^2}
  \pdfSimple dy \\
  & = E_Y[\llSimpleSecondDeriv] + E_Y[\scoreSimpleCond^2]
  \end{split}
\end{equation}
by the product rule of differentiation, and using the fact
$\llSimpleFirstDeriv = \frac{f'(y;\thetaOneOD)}{\pdfSimple} \implies
f'(y;\thetaOneOD) = \llSimpleFirstDeriv \pdfSimple$. As in
Section \ref{sec:exp-zero-proof}, standard regularity assumptions
allow swapping the order of differentiation and integration.

As $E_Y [\scoreSimple] = 0 \implies \deltaThetaOneOD
E_Y [\scoreSimple] = 0$, we therefore have:
\begin{equation}
  E_Y[\llSimpleSecondDeriv] + E_Y[\scoreSimpleCond^2] = 0
  \iff E_Y[\scoreSimple^2] = -E_Y[\llSimpleSecondDeriv]
\end{equation}

That is, $\text{Var}\lrbr{\scoreSimple} = \mathcal{I}(\thetaOneOD)$,
by definition of the Fisher information. Once again, similar
reasoning holds as model and data dimension increases -- thus,
generally, $\text{Var}\lrbr{\boldsymbol{u}_1 (\thetaOne)} =
\mathcal{I}(\thetaOne)$. 

\subsubsection{LRT and S equivalence}
\label{sec:lrt-s-equiv-proof}

Below, we use $\ell(\thetaOneOD)$ in place of $\llSimple$ for
brevity. We also use $\ellT$ to represent a second-order Taylor series
expansion of $\ell(\thetaOneOD)$, centered at
$\thetaOneTildeOD$. (Recall, $\thetaOneTildeOD$ is the parameter vector
specified under $H_0$, as described in Section
\ref{sec:calculating-score-stat}.) If $\thetaHatStar$ represents the
peak of this quadratic approximation, the approximation-based LRT
statistic comparing the $H_0$ and $H_1$ models is:
$2\lrbr{\ell(\thetaHatStar) - \ell(\thetaOneTildeOD)}$. 

And as seen in Section \ref{sec:calculating-score-stat}, when
$\thetaOneOD$ is one-dimensional, the score statistic is given by:
$\frac{\scoreUOneDim^2}{\mathcal{I}(\thetaOneTildeOD)}$. We will show
these two expressions are equivalent.

The aforementioned second-order Taylor series expansion is
given by: 
\begin{equation}
  \label{eq:taylor-exp}
  \ellT = \ellTilde + \ellTildeFirstDeriv(\thetaOneOD -
  \thetaOneTildeOD) +  \frac{1}{2} \ellTildeSecondDeriv(\thetaOneOD -
  \thetaOneTildeOD)^2 
\end{equation}

The gradient of this parabola is $\ellT' = \ellTildeFirstDeriv +
\ellTildeSecondDeriv(\thetaOneOD - \thetaOneTildeOD)$ (recall under a
second-order Taylor series expansion, derivatives with
order greater than $2$ are negligible). As $\thetaHatStar$
maximises the approximation (i.e. sets the gradient to $0$),
\begin{equation}
  \label{eq:theta-hat-star}
  \thetaHatStar = \thetaOneTildeOD -
  \frac{\ellTildeFirstDeriv}{\ellTildeSecondDeriv}
\end{equation}

Substituting Equation \ref{eq:theta-hat-star} into
Equation \ref{eq:taylor-exp}, 
\begin{equation}
  \ell_1(\thetaHatStar) =  \ellTilde -
  \frac{\ellTildeFirstDeriv^2}{2\ellTildeSecondDeriv} \iff
  2\lrbr{\ell(\thetaHatStar) - \ell(\thetaOneTildeOD)} =
  \frac{\ellTildeFirstDeriv^2}{-\ellTildeSecondDeriv}  
\end{equation}

That is, $2\lrbr{\ell(\thetaHatStar) - \ell(\thetaOneTildeOD)} =
\frac{\scoreUOneDim^2}{\mathcal{I}(\thetaOneTildeOD)}$, by definition
of the score vector and Fisher information. Once again,
similar reasoning holds as model and data dimension increase. 

\subsection{Score test example}
\label{sec:score-test-rtmb-ex}

\subsubsection{RTMB implementation}
\label{sec:rtmb-appendix}

%% Code is from `RCode.R' in 'Score-test-paper' folder :) -- just note
%% that in code here, have replaced 'beta0' with 'alpha' and 'beta1'
%% with 'beta'

We use the simple linear regression example from Section
\ref{sec:calculating-score-stat}, where $Y_i \sim \text{Normal}(\mu_i,
1)$, with $H_0: \mu_i = \alpha$ and $H_1: \mu_i =
\alpha + \beta x_i$. Using AD to fit the $H_0$ model in this
simple example does not necessarily provide a time saving, but is done
for illustrative purposes. Apart from the objective function and
its corresponding parameters, the code below generally applies
to calculating score statistics for different models.  

First, we define the objective
function (the negative log-likelihood) for the $H_0$ and $H_1$
model. 

\begin{lstlisting}[language=R]
nll <- function(pars) {
-sum(dnorm(y, pars$alpha + pars$beta*x, 1, log = TRUE))
}
\end{lstlisting}

Then, we create \lstinline{RTMB} objects for the $H_0$ and $H_1$ models. These
 contain exact derivatives, amongst other information on the
objective functions. 

\begin{lstlisting}[language=R]
obj.null <- MakeADFun(nll, list(alpha = alpha.start, beta = 0), map =
list(beta = factor(NA))) 
obj.alt <- MakeADFun(nll, list(alpha = alpha.start, beta = beta.start))
\end{lstlisting}

Next, we obtain the $H_0$ MLEs and define $\thetaOneTilde$. 

\begin{lstlisting}[language=R]
fit.null <- nlminb(obj.null$par, obj.null$fn, obj.null$gr)
pars.null <- list(alpha = fit.null$par["alpha"], beta = 0)
\end{lstlisting}

In place of symbolic differentiation, the following two simple lines of code
calculate $\scoreU$ and $\scoreI^{-1}$. 

\begin{lstlisting}[language=R]
score.null <- -obj.alt$gr(pars.null)
info.null <- obj.alt$he(pars.null)
\end{lstlisting}

And lastly, we obtain the score statistic and its $p$-value. 

\begin{lstlisting}[language=R]
S <- score.null %*% solve(info.null) %*% t(score.null)
p.val <- 1 - pchisq(S, df=1)
\end{lstlisting}

\subsubsection{Symbolic differentiation}
\label{sec:symb-diff-appendix}

To find $S$ via symbol differentiation, the few lines of code from above
are replaced by the calculations below. As mentioned in Section
\ref{sec:calculating-score-stat}, this example is simple. In reality,
similar calculations are likely to be much more complicated, if not
impossible. 

Recall that $Y_i \sim \text{Normal}(\mu_i,
1)$, with $H_0: \mu_i = \alpha$ and $H_1: \mu_i =
\alpha + \beta x_i$. We define the corresponding parameter
vectors as $\thetaZero = \lr{\betaZero}$ and $\thetaOne = \lr{\betaZero,
  \betaOne}$, respectively. We also denote the corresponding
log-likelihood values as $\ellZero$ and $\ellOne$.

To begin, we find the $H_0$ MLE. The pdf for one observed $Y_i$ value is
given by:

\begin{equation}
  f \left(y_i |  \thetaZero \right) = \frac{1}{\sqrt{2 \pi}} \exp
  \lrbr{\frac{-(y_i - \mu_i)^2}{2}}
\end{equation}

Therefore, the log-likelihood under $H_0$ is given by: 

\begin{equation}
  \ellZero \left(\thetaZero | \yBold \right) = \sumN \log
  \lr{\frac{1}{\sqrt{2 \pi}}} - \frac{(y_i - \betaZero)^2}{2} = \sumN
  - \frac{1}{2} \log(2 \pi) - \frac{(y_i - \betaZero)^2}{2}
\end{equation}

\begin{equation}
  \implies \frac{\delta \ellZero}{\delta \alpha} = \sumN -
  \frac{\delta}{\delta \betaZero} \lrbr{\frac{(y_i - \betaZero)^2}{2}}
  = \sumN - \frac{2 (y_i - \betaZero) (-1)}{2} = \sumN (y_i - \betaZero)
\end{equation}

And so,

\begin{equation*}
  \begin{split}
  \frac{\delta \ellZero}{\delta \betaZero} = 0 & \iff \sumN
  (y_i - \betaZeroMLE) = 0 \\
  \implies \frac{\delta \ellZero}{\delta \betaZero} = 0 & \iff \sumN y_i = n \betaZeroMLE
  \end{split}
\end{equation*}

\begin{equation}
  \implies \betaZeroMLE = \frac{\sumN y_i}{n} = \yBar
\end{equation}

Next, we find the score vector and observed information matrix of the
$H_1$ log-likelihood, evaluated 
at $\thetaOneTilde$. As mentioned in Section
\ref{sec:calculating-score-stat}, $\thetaOneTilde = \left(
  \hat{\alpha}, 0 \right) = (\yBar, 0)$. The $H_1$ log-likelihood is given by:
\begin{equation}
  \ellOne \left( \thetaOne | \yBold \right) = \sumN - \frac{1}{2} \log(2
  \pi) - \frac{(y_i - \betaZero - \betaOne x_i)^2}{2}
\end{equation}

And so,

\begin{equation}
  \frac{\delta \ellOne}{\delta \betaZero} = \sumN -
  \frac{\delta}{\delta \betaZero} \frac{(y_i - \betaZero - \betaOne
    x_i)^2}{2} = \sumN - \frac{2 (y_i - \betaZero - \betaOne x_i)
    (-1)}{2} = \sumN (y_i - \betaZero - \betaOne x_i)
\end{equation}

\begin{equation}
  \frac{\delta \ellOne}{\delta \betaOne} = \sumN -\frac{\delta}{\delta
  \betaOne} \frac{(y_i - \betaZero - \betaOne x_i)^2}{2} = \sumN -
\frac{2 (y_i - \betaZero - \betaOne x_i) (-1)}{2} = \sumN x_i (y_i -
\betaZero - \betaOne x_i) 
\end{equation}

That is,
\begin{equation*}
  \boldsymbol{u}_1 (\thetaOne) =
  \begin{bmatrix}
    \sumN(y_i - \betaZero - \betaOne x_i) \\
    \sumN x_i (y_i - \betaZero - \betaOne x_i)
  \end{bmatrix}
  \implies
  \boldsymbol{u}_1 (\thetaOneTilde) =
  \begin{bmatrix}
    \sumN(y_i - \yBar) \\
    \sumN x_i (y_i - \yBar)
  \end{bmatrix}
\end{equation*}

And since $\sumN (y_i - \yBar) = 0$, we can write:
\begin{equation}
  \boldsymbol{u}_1 (\thetaOneTilde) =
  \begin{bmatrix}
    0 \\
    \sumN x_i (y_i - \yBar)
  \end{bmatrix}
\end{equation}

To find the observed information matrix, we begin with the following
second partial derivatives.

\begin{equation}
  \frac{\delta^2 \ellOne}{\delta \betaZero^2} = \sumN \deltaBetaZero (y_i -
  \betaZero - \betaOne x_i) = \sumN (-1) = -n
\end{equation}

\begin{equation}
  \frac{\delta^2 \ellOne}{\delta \betaZero \delta \betaOne} =
  \sumN
  \deltaBetaOne (y_i - \betaZero - \betaOne x_i) = - \sumN x_i
\end{equation}

\begin{equation}
  \frac{\delta^2 \ellOne}{\delta \betaOne \delta \betaZero} =
  \frac{\delta^2 \ellOne}{\delta \betaZero \delta \betaOne} = - \sumN x_i
\end{equation}

\begin{equation}
  \frac{\delta^2 \ellOne}{\delta \betaOne^2} = \sumN \deltaBetaOne x_i (y_i -
  \betaZero - \betaOne x_i) = - \sumN x_i^2
\end{equation}

Therefore, the negative hessian is given by:
\begin{equation}
  - \frac{\delta^2 \ellOne}{\delta \thetaOne \delta \thetaOne^T} = -
  \begin{bmatrix}
    -n & - \sumN x_i \\
    - \sumN x_i & - \sumN x_i^2
  \end{bmatrix} =
  \begin{bmatrix}
    n & \sumN x_i \\
    \sumN x_i & \sumN x_i^2
  \end{bmatrix}
\end{equation}

As the negative hessian does not depend on $\thetaOne$, we have:  
\begin{equation}
  \mathcal{I}_1 (\thetaOneTilde) =
  \begin{bmatrix}
    n & \sumN x_i \\
    \sumN x_i & \sumN x_i^2
  \end{bmatrix}
\end{equation}

To calculate the score statistic, we will need the inverse of the
observed information matrix. 
\begin{equation}
  \scoreI^{-1} = \frac{1}{n \sumN x_i^2 -
    \lr{\sumN x_i}^2}
  \begin{bmatrix}
    \sumN x_i^2 & - \sumN x_i \\
    - \sumN x_i & n
  \end{bmatrix}
\end{equation}

Lastly, we calculate $S = \scoreU^T \scoreI^{-1} \scoreU$,
\begin{equation}
  S  = \frac{1}{n \sumN x_i^2 -
    \lr{\sumN x_i}^2} \lr{
  \begin{bmatrix}
    0 & \sumN x_i (y_i - \yBar)
  \end{bmatrix}
  \begin{bmatrix}
    \sumN x_i^2 & - \sumN x_i \\
    - \sumN x_i & n
  \end{bmatrix}
  \begin{bmatrix}
    0 \\
    \sumN x_i (y_i - \yBar)
  \end{bmatrix}
  }
\end{equation}

where:

\begin{equation*}
  \begin{bmatrix}
    0 & \sumN x_i (y_i - \yBar)
  \end{bmatrix}
  \begin{bmatrix}
    \sumN x_i^2 & - \sumN x_i \\
    - \sumN x_i & n
  \end{bmatrix} =
  \begin{bmatrix}
    -\sumN x_i \sumN x_i(y_i - \yBar) & n \sumN x_i (y_i - \yBar)
  \end{bmatrix}
\end{equation*}

Therefore: 

\begin{equation*}
  S = \frac{1}{n \sumN x_i^2 -
    \lr{\sumN x_i}^2} \lr{
    \begin{bmatrix}
      -\sumN x_i \sumN x_i(y_i - \yBar) & n \sumN x_i (y_i - \yBar)
    \end{bmatrix}
    \begin{bmatrix}
      0 \\
      \sumN x_i (y_i - \yBar)
    \end{bmatrix}
  }
\end{equation*}

\begin{equation}
  \implies S =
  \frac{n \lrsq{\sumN x_i (y_i - \yBar)}^2}{n \sumN x_i^2 - \lr{\sumN x_i}^2}
\end{equation}

$S$ can be compared to the $\chi^2_1$ distribution using an R function call
similar to the one in Section \ref{sec:rtmb-appendix}. 

\subsection{Brief  overview of SCR and the candidate set}
\label{sec:overview-candidate-models}

% $D$ describes animal density, while $g_0$ and $\sigma$ describe 
% the probability of being captured.

Using records of when and where individuals were captured, SCR models
allow for estimation of animal density and distribution
\parencite{borchersEfford2008}. As described in \textcite{secr-book},
there are two parts to an SCR model: the state model and the
observation model. The state model describes the `biological reality
we wish to describe', the distribution of the animal population. The observation model
describes the sampling process we use to understand this
population. Selecting accurate state and observation models enhances
the validity of inference. We briefly describe our
selections below. Please refer to texts such as \textcite{secr-book}
for further details. 

Specifying the state model means choosing a spatial point process to
describe the distribution of animals in the survey area
\parencite{borchersFewster2016}. It is common to select a spatial
Poisson process \parencite{secr-book}. Specifically, following the
example of \parencite{borchersEfford2008}, we use a
homogeneous Poisson process, so animals are assumed to be
distributed uniformly across the survey area for each year. Therefore, we
estimate a constant density parameter $D$ for each year. As we see
below, for some models we assume animal density is constant across all
years. For others, we assume density varies by year. 

To specify the observation process, we select a detection
function. This describes how an animal's probability of detection 
varies as a function of the distance between their activity centre and a
detector, where an animal's activity centre is the centre of their home
range \parencite{secr-book}. The choice of detection function dictates
the `detection' parameters 
to be estimated. Although \textcite{borchersEfford2008} consider
SCR models with half-normal and hazard rate detection functions, we
only consider models with a 
half-normal detection function, which has parameters $g_0$ and
$\sigma$. Hazard rate detection functions are seldom used and
generally not recommended due to their long tail
\parencite{secr-pkg}.

When defining a SCR model, it is standard to specify effects
(e.g. behavioural effects, covariate effects, etc.) for the parameters
of interest, which are $D$, $g_0$ and $\sigma$ in our
case. Therefore, as in \textcite{borchersEfford2008}, we consider
models with the effects shown in Table \ref{table:dependencies},
specified using the parlance of the \lstinline{secr}
package. Considering all possible combinations of
these parametrisations yields our forty five candidate models.

\begin{table}[h!]
  \centering
\begin{tabular} { |c|c|c|c|  }
  \hline
  \textbf{$D$} & \textbf{$\sigma$} &
                                             \multicolumn{2}{|c|}{\textbf{$g_0$}} 
  \\
  \hline
  $D \sim 1$ & $\sigma \sim 1$  & $g_0 \sim 1$ & $g_0 \sim \text{b}$ \\
  \hline
  $D \sim \text{session}$ & $\sigma \sim \text{session}$ & $g_0 \sim
                                                           \text{session}$
             &  $g_0 \sim \text{session} + \text{b}$ \\
  \hline
  $D \sim \text{Session}$ & $\sigma \sim \text{h2}$ & $g_0 \sim
                                                      \text{h2}$ & \\
  \hline
\end{tabular}
\caption{All parametrisations considered in our candidate set.}
\label{table:dependencies}
\end{table}

For SCR models, it is standard to refer to each survey
occasion (in our case, each survey year) as a `session'. Therefore, in Table
\ref{table:dependencies} (see \textcite{secr-book} for further detail):
\begin{itemize}
\item `$\sim 1$' represents a parameter being held constant across
  sessions. For example, with $D \sim 1$, we have only one
  estimate of $D$ and use it for all sessions;
\item `$\sim \text{session}$' means a parameter has session-specific
  levels. For example, with $D \sim \text{session}$, we have a separate estimate
  of $D$ for each session;
\item `$\sim \text{Session}$' represents a trend across
  sessions. With `$D \sim \text{Session}$', we  have $D(y) =
  exp(\beta_0 + \beta_1 y)$, 
  where $y$ is the session number \parencite{borchersEfford2008}. In
  our case, $y=1,...,5$;
\item `$\sim \text{h2}$' denotes a
  two-class finite mixture model. For example, with `$\sigma \sim \text{h2}$', we assume
  there are two groups in the population: Group 1 uses $\sigma_1$,
  and Group 2 uses $\sigma_2$. An 
  individual falls in Group 1 with probability $p$ and in Group 2
  with probability $(1-p)$. These estimates are pooled across sessions; 
\item `$\sim \text{b}$' represents a behavioural response to capture. With $g_0 \sim
  \text{b}$, we have two estimates of $g_0$: one for individuals
  captured previously in a session, and one for individuals
  not yet captured in a session. These estimates are pooled
  across sessions;
\item `$\sim \text{session} + \text{b}$' means we have
  session-specific levels and a trap response -- e.g. with $g_0 \sim \text{session} +
  \text{b}$, we fit a model with two separate estimates of $g_0$ for
  each session: one for previously captured individuals, and one for
  individuals not caught yet. 
\end{itemize}

\subsection{AIC results}
\label{sec:aic-res-app}

\begin{table}[h!]
  \caption{AIC values for all 45 models in the candidate set, where
    all models are fitted and AIC is calculated using the
    \lstinline{secr} package. The `Model' column contains the model
    description in \lstinline{secr} parlance, `Number of parameters'
    provides the number of model parameters and `$\Delta$AIC'
    represents the difference in AIC from the final selected model
    (model with the lowest AIC). All values are reported to 1 dp,
    except for when this would result in identical $\Delta$AIC values.} 
  \label{table:aic-table}
\centering
\begin{tabularx}{0.8\textwidth} { 
    >{\centering\arraybackslash \hsize=1.5\hsize}X 
    >{\centering\arraybackslash \hsize=0.5\hsize}X 
    >{\centering\arraybackslash \hsize=1\hsize}X  }
  \hline
  \hline
  Model & Number of parameters & $\Delta$AIC \\
  \hline
  \Dconst, \gbehav, \sigmaconst & 4 & 0 \\
  \DSession, \gbehav, \sigmaconst & 5 & 0.97 \\
  \Dconst, \gbehav, \sigmamix & 6 & 1.3 \\
  \Dconst, \gbehav, \sigmasession & 8 & 2.0 \\
  \DSession, \gbehav, \sigmamix & 7 & 2.3 \\
  \Dconst, \gsessionbehav, \sigmaconst & 8 & 2.7 \\
  \DSession, \gbehav, \sigmasession & 9 & 4.0 \\
  \DSession, \gsessionbehav, \sigmaconst & 9 & 4.5 \\
  \Dconst, \gsessionbehav, \sigmamix & 10 & 4.8 \\
  \Dsession, \gbehav, \sigmaconst & 8 & 4.8 \\
  \Dsession, \gbehav, \sigmamix & 10 & 6.1 \\
  \Dconst, \gconst, \sigmaconst & 3 & 6.4 \\
  \Dsession, \gbehav, \sigmasession & 12 & 6.6 \\
  \DSession, \gsessionbehav, \sigmamix & 11 & 6.7 \\
  \Dconst, \gmix, \sigmaconst & 5 & 7.1 \\
  \DSession, \gconst, \sigmaconst & 4 & 7.3 \\
  \Dconst, \gsessionbehav, \sigmasession & 12 & 7.7 \\
  \DSession, \gmix, \sigmaconst & 6 & 8.0 \\
  \Dconst, \gconst, \sigmamix & 5 & 8.1 \\
  \DSession, \gconst, \sigmamix & 6 & 8.95 \\
  \Dsession, \gsessionbehav, \sigmaconst & 12 & 8.97 \\
  \Dconst, \gmix, \sigmamix & 6 & 9.37 \\
  \Dconst, \gconst, \sigmasession & 7 & 9.4 \\
  \DSession, \gsessionbehav, \sigmasession & 13 & 9.66 \\
  \Dconst, \gsession, \sigmaconst & 7 & 9.7 \\
  \DSession, \gmix, \sigmamix & 7 & 10.2 \\
  \Dconst, \gmix, \sigmasession & 9 & 11.0 \\
  \Dsession, \gconst, \sigmaconst & 7 & 11.2 \\
  \DSession, \gconst, \sigmasession & 8 & 11.36 \\
  \Dsession, \gsessionbehav, \sigmamix & 14 & 11.4 \\
  \Dconst, \gsession, \sigmamix & 9 & 11.5 \\
  \DSession, \gsession, \sigmaconst & 8 & 11.6 \\
  \Dsession, \gmix, \sigmaconst & 9 & 11.9 \\
  \Dsession, \gmix, \sigmamix & 10 & 11.91 \\
  \Dsession, \gsessionbehav, \sigmasession & 16 & 12.1 \\
  \Dsession, \gconst, \sigmamix & 9 & 12.85 \\
  \DSession, \gmix, \sigmasession & 10 & 12.9 \\
  \DSession, \gsession, \sigmamix & 10 & 13.5 \\
  \Dsession, \gconst, \sigmasession & 11 & 13.9 \\
  \Dconst, \gsession, \sigmasession & 11 & 14.9 \\
  \Dsession,  \gmix, \sigmasession & 13 & 15.3 \\
  \Dsession, \gsession, \sigmaconst & 11 & 16.3 \\
  \DSession, \gsession, \sigmasession & 12 & 16.9 \\
  \Dsession, \gsession, \sigmamix & 13 & 18.5 \\
  \Dsession, \gsession, \sigmasession & 15 & 19.7 \\
  \hline
\end{tabularx}
\end{table}

\newpage

\printbibliography

\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
